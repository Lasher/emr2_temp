import { UserKeyPressDirective } from './user-key-press.directive';

describe('UserKeyPressDirective', () => {
  it('should create an instance', () => {
    const directive = new UserKeyPressDirective();
    expect(directive).toBeTruthy();
  });
});
